const age = '<p id="viewer-card__profile-age"></p>';

const history = `
    <div class="tw-inline-flex tw-pd-1">
        <select class="tmt-name-history">
            <option>Name History</option>
        </select>
    </div>
`;

export default {
    age,
    history
};
